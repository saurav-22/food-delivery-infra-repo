apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-service
  namespace: food-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: menu-service
  template:
    metadata:
      labels:
        app: menu-service
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: node-type
                    operator: In
                    values:
                      - ondemand
      containers:
        - name: menu-service
          image: "{{ .Values.images.menuService.repository }}:{{ .Values.images.menuService.tag }}"
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
          resources:
{{- toYaml .Values.resources | nindent 12 }}
