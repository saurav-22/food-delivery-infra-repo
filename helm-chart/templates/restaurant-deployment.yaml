apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
  namespace: food-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: node-type
                    operator: In
                    values:
                      - ondemand
      containers:
        - name: restaurant-service
          image: "{{ .Values.images.restaurantService.repository }}:{{ .Values.images.restaurantService.tag }}"
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
          resources:
{{- toYaml .Values.resources | nindent 12 }}
